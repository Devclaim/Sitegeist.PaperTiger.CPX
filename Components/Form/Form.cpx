from "./FormProps.cpx" import { FormProps }

export component Form {
  form: FormProps
  error: slot
  content: slot

  render <form
    id={form.id}
    class="papertiger-form__element"
    data-form-mode={form.formMode}
    action={form.action}
    method={form.method}
    novalidate={form.noValidate}
    enctype="multipart/form-data"
  >
    {error}
    {content}
  </form>
}
