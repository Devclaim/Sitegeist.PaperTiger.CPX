from "./HiddenFieldProps.cpx" import { HiddenFieldProps }

export component HiddenField {
  field: HiddenFieldProps

  render <input
    type="hidden"
    name={field.name}
    value={field.value}
    class="papertiger-field__control papertiger-field__control--hidden"
  />
}
